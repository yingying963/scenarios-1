# Note

- Input the `docker create --name my-nginx3 -e NGINX_HOST=example.com nginx` command to create a new container called `my-nginx3` from the `nginx` image and set the `NGINX_HOST` environment variable to `example.com` in the container.
